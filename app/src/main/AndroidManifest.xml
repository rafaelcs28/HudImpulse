<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Auto-update -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Speed limit via HERE Maps (GPS location) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Shizuku provider query permission -->
    <queries>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="HUD Impulse"
        android:theme="@style/Theme.HudImpulse"
        android:supportsRtl="true">

        <!-- Shizuku provider — required for ShizukuBinderWrapper to work -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- FileProvider for APK install during auto-update -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" /></provider>

        <!-- Settings screen (not the launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="HUD Impulse Settings" />

        <!-- HUD Display: launcher — opens directly at 480×240, all black until nav data arrives -->
        <activity
            android:name=".HudDisplayActivity"
            android:exported="true"
            android:theme="@style/Theme.HudDisplay"
            android:label="HUD Impulse"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background service: reads car speed + TSR limit via Beantechs AIDL (Shizuku) -->
        <service
            android:name=".CarDataService"
            android:exported="false" />

        <!-- Navigation data receiver from Headunit Revived -->
        <receiver
            android:name=".NavigationReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.andrerinas.headunitrevived.NAVIGATION_UPDATE" />
            </intent-filter>
        </receiver>

        <!-- Auto-start HUD when Android Auto connects via Headunit Revived -->
        <receiver
            android:name=".AutoStartReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.andrerinas.headunitrevived.ACTION_CONNECTED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
